#include <bits/stdc++.h>
using namespace std;


long factorial(const int n)
{
    long f = 1;
    for (int i=1; i<=n; ++i)
        f *= i;
    return f;
}

long find_numbers(int n){
    
}

int main(){
    int n;
    cin >> n;
    int a[n];
    for(int i = 0; i < n; i++){
        cin >> a[i];
    }
    int per = 0;
    __is_permutation
    while (per < pow(10,5))
    {
        
    }
    
    
    return 0;
}